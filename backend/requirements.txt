# =============================================================================
# CareerOS Backend — Core Dependencies (Phase 1)
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Configuration ---
pydantic[email]>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# --- Database ---
sqlalchemy>=2.0.30
asyncpg>=0.29.0           # async PostgreSQL driver
psycopg2-binary>=2.9.9   # sync driver for Alembic
alembic>=1.13.0
pgvector>=0.3.0           # pgvector SQLAlchemy type support

# --- Authentication ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2
python-multipart>=0.0.9   # required for OAuth2 form data

# --- Background Jobs ---
celery>=5.4.0
redis>=5.0.4              # Redis client + Celery broker

# --- HTTP ---
httpx>=0.27.0             # async HTTP client (used in tests + OAuth)

# --- Logging ---
python-json-logger>=2.0.7

# --- File handling (Phase 2+ but abstraction established now) ---
boto3>=1.34.0             # S3 storage backend
aiofiles>=23.2.1          # async file I/O for local storage
